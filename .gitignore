# Pipeline artifacts -- regenerable by re-running the relevant stage (see CLAUDE.md)
outputs/
images/

# Overleaf-synced report repo -- version-controlled separately via its own git remote
report/

# Supervisor-meeting scratch assets (large GIFs, unrelated PDFs) -- not part of the reproducible pipeline
presentation/

# Python bytecode cache
__pycache__/
*.pyc

# Jupyter checkpoints
.ipynb_checkpoints/

# Editor/workspace settings
.vscode/

# Internal documentation -- kept locally, not published on GitHub (README.md is the public-facing doc)
CLAUDE.md
COMPILED.md
LOGIC.md
docs/

# Not required on GitHub -- kept locally only
data_visualisation.ipynb
